<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
      SEND_SMS ist der Grund, warum diese App per Sideload verteilt wird und
      nicht ueber den Play Store: Google gibt die Berechtigung nur noch
      Standard-SMS-Apps. Fuer die Verteilung als selbst signiertes APK
      innerhalb der Feuerwehr ist das unproblematisch.
    -->
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!--
      Fuer den Update-Kanal: die App laedt ihr eigenes Update vom
      Feuerwehr-Server und uebergibt es dem System. Android zeigt dabei
      trotzdem seinen Installationsdialog - eine stille Installation ist
      damit NICHT moeglich und auch nicht gewollt.
    -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- Der Versand laeuft sichtbar als Vordergrunddienst, nicht heimlich. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <uses-feature
        android:name="android.hardware.telephony"
        android:required="true" />

    <application
        android:name=".app.FfSmsApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FfSms">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          Nimmt die SENT-/DELIVERED-Rueckmeldungen des Modems entgegen.
          exported=false: die Intents kommen ausschliesslich aus unseren
          eigenen PendingIntents.
        -->
        <receiver
            android:name=".send.SmsStatusReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="cc.netwokx.ffsms.SMS_SENT" />
                <action android:name="cc.netwokx.ffsms.SMS_DELIVERED" />
            </intent-filter>
        </receiver>

        <!-- Ergebnis der Installationssitzung, inkl. Rueckfrage an den Benutzer. -->
        <receiver
            android:name=".update.InstallStatusReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="cc.netwokx.ffsms.INSTALL_STATUS" />
            </intent-filter>
        </receiver>

        <!--
          WorkManager bringt diesen Service selbst mit. Ab Android 14 muss ein
          Vordergrunddienst seinen Typ deklarieren, sonst bricht setForeground()
          mit einer MissingForegroundServiceTypeException ab.
        -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />

    </application>
</manifest>
